int floor(int a[],int n)
{
	int l=0;
	int r=n-1;
	int floor;
	while(l<e)
	{
		int mid=(l+r)/2;
		if(x==a[mid]) return a[mid];
		
		else if (x<a[mid]) r=mid-1;
		
		else
		{
			floor=a[mid];
			l=mid+1;
		}
	}
	return floor;
}
