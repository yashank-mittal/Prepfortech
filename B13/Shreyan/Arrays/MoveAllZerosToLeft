# python solution 

nums=[1,3,0,12,25,1,0]

nonZeroPos=len(nums)-1;
for i in range(len(nums)-1,-1,-1):
    if nums[i]!=0:
        nums[nonZeroPos]=nums[i]
        nonZeroPos-=1
while nonZeroPos>=0:
    nums[nonZeroPos]=0
    nonZeroPos-=1
